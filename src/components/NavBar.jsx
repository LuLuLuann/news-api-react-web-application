function NavBar() {
    return (
        <nav style={styles}>
            <a href=""></a>
            <a href=""></a>
        </nav>
    );
}

const styles = {
    display: "flex",
    justifyContent: "space-around"
}

export default NavBar;